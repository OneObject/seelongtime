
launcher='org.apache.catalina.startup.Bootstrap'
PID=`ps -ef | grep $launcher | grep s2urvey | grep -v ' grep' | awk '{print $2}'`

echo $PID

if [ ! -e $PID ];
then
        echo "kill tomcat-s2urvey the PID is $PID"
        kill $PID
else
        echo "tomcat-s2urvey the not run"
fi